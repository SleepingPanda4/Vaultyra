services:
  vaultyra:
    build: .
    container_name: vaultyra
    restart: unless-stopped
    ports:
      - "12450:12450"
    environment:
      NODE_ENV: production
      PORT: 12450
      HOSTNAME: 0.0.0.0
    volumes:
      - vaultyra-data:/app/data

volumes:
  vaultyra-data:
