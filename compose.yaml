services:
  vaultyra:
    build: .
    container_name: vaultyra
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    volumes:
      - vaultyra-data:/app/data

volumes:
  vaultyra-data:
